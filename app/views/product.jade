extends layout

block content

  h1 Product
  if user
    h2= user.firstname

  .well.product
    .row
      .col-md-4
        img(src= product.photo.url)
      .col-md-8
        h3= product.title
        p= product.content
        p= product.price
        p= product.category
        p= product.tags